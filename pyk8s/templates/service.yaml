apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyk8s.fullname" . }}
  labels:
    {{- include "pyk8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyk8s.selectorLabels" . | nindent 4 }}
